runner:
  phpcs:
    cmd: ./vendor/bin/phpcs --standard=./phpcs.xml --report=emacs ./src/PhpSample.php
    errorformat:
      - "%f:%l:%c: %trror - %m"
      - "%f:%l:%c: %tarning - %m"
    level: warning
  textlint:
    cmd: npm run textlint
    errorformat:
      - "%E%f: line %l, col %c, Error - %m"
      - "%C%m"
  deno_lint:
    cmd: deno lint --compact ./src/ESLintSample.ts
    errorformat:
      - "%E%f: line %l, col %c - %m"
